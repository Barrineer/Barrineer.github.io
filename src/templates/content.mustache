<main>
{{#header}}
    <h2>{{title}}</h2>
    {{#description}}
        <small>{{.}}</small>
    {{/description}}
    {{#section}}
        <section>
            <h3>{{subtitle}}</h3>
            {{#project}}
            <div>
                <h4>{{name}}</h4>
                <p>{{description}}</p>
            </div>
            {{/project}}
        </section>
    {{/section}}
{{/header}}
</main>
